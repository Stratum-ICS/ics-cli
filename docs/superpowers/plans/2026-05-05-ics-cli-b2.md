# B2 — Pull server state + conflict policy Implementation Plan (Rust)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Refresh local markdown from Stratum (read/list APIs per pinned OpenAPI), update frontmatter/index, and enforce **explicit `--policy`** flags — no silent overwrites.

**Architecture:** Add **`src/sync/pull.rs`** (or `src/pull.rs`) using **`StratumClient`** from B1. CLI uses **clap** `ValueEnum` for `take-mine` / `take-theirs` (exact strings TBD in help text).

**Tech Stack:** Same as B1 (**tokio** + **reqwest**). Optional **similar** or **diff** crate for user-visible diffs when refusing `take-mine`.

**Prerequisite:** B1 complete; `routes.rs` extended with GET/list note paths from OpenAPI.

---

### Task 1: Refresh route constants

- [ ] Re-run B1 OpenAPI capture; extend **`src/stratum/routes.rs`** with read endpoints.

- [ ] **Commit:** `git commit -m "chore: refresh stratum route constants for read apis"`

---

### Task 2: Fetch + apply

**Files:**
- Create: `src/sync/mod.rs`, `src/sync/pull.rs`

- [ ] **Step 1:** `async fn fetch_note_body(...) -> Vec<u8>` using pinned routes.

- [ ] **Step 2:** `apply_server_to_worktree(repo_root, rel_path, body, policy) -> Result<(), PullError>` — **`thiserror`** enum for conflict refusal.

- [ ] **Step 3:** **`#[tokio::test]`** + **wiremock** for success and `take-mine` abort.

- [ ] **Commit**

```bash
git commit -m "feat: pull note body with conflict policies"
```

---

### Task 3: `ics pull`

- [ ] **Step 1:** Clap: `ics pull [--all-tracked | PATHS...] --policy <ENUM>`.

- [ ] **Step 2:** Integration test with temp repo + mocks.

- [ ] **Commit:** `git commit -m "feat: ics pull command"`

---

### Task 4: Dirty working tree guard

- [ ] **Step 1:** Reuse B0 **status** diff logic in library API — if working tree ≠ `HEAD` and user runs `pull`, exit **`ExitCode::from(2)`** unless `--force`.

- [ ] **Step 2:** Test dirty abort.

- [ ] **Commit:** `git commit -m "fix: guard pull when working tree differs from head"`

---

### Plan self-review

Conflict policy explicit (clap). Dirty-tree behavior aligned with B0 store.

---

**Next:** [`2026-05-05-ics-cli-c.md`](2026-05-05-ics-cli-c.md) when vault APIs stable.
